# OJOZ - dependencias de Python.
# Versiones fijadas a las que estan verificadas en el entorno de desarrollo.
#
# Requisito externo NO instalable por pip:
#   Tesseract OCR  ->  https://github.com/UB-Mannheim/tesseract/wiki
#   Instalar con el paquete de idioma español ("spa").
#   Por defecto se busca en C:\Program Files\Tesseract-OCR\tesseract.exe;
#   se puede cambiar con la variable de entorno TESSERACT_CMD.

# --- Interfaz ---
flet==0.86.5
flet-desktop==0.86.5

# --- Audio: sintesis y reconocimiento de voz ---
gTTS==2.5.4
pyttsx3==2.99
pygame==2.6.1
SpeechRecognition==3.17.0
PyAudio==0.2.14
pocketsphinx==5.1.1

# --- Inferencia de IA local (DTLN + ArcFace) ---
# La variante GPU incluye CUDA/cuDNN y conserva CPUExecutionProvider como
# respaldo. El modelo facial se descarga una vez antes de la primera demo.
onnxruntime-gpu[cuda,cudnn]==1.29.0

# --- Vision por computador, reconocimiento facial y OCR ---
insightface==1.0.1
opencv-python==5.0.0.93
numpy==2.5.1
imutils==0.5.4
pytesseract==0.3.13
pillow==12.3.0

# --- Conversacion por modelo de lenguaje (opcion B, desactivada por defecto) ---
# Solo se usa si se activa con OJOZ_LLM=1 y existe ANTHROPIC_API_KEY.
# Sin ella el asistente funciona con el enrutador por palabras clave de siempre.
anthropic==1.0.0

# --- Utilidades ---
colorama==0.4.6

# --- Empujar para hablar (tecla espacio) ---
# Flet no reenvia la repeticion de una tecla mantenida (Flutter la separa en
# un evento aparte que Flet no expone), asi que la deteccion de "soltar" se
# hace a nivel de sistema operativo con esta libreria.
pynput==1.8.2

# --- Seleccion de camara por nombre ---
# Permite elegir la webcam fisica del laptop por nombre (p. ej. "UVC WebCam")
# en vez de un indice de DirectShow fijo, que cambia segun que otras camaras
# virtuales (iVCam, OBS, NVIDIA Broadcast) esten activas en ese momento.
pygrabber==0.2

# --- Variables de entorno desde archivo ---
# Carga las claves de API (ELEVENLABS_*, ANTHROPIC_API_KEY, GOOGLE_VISION_API_KEY,
# OJOZ_*) desde un archivo .env junto a main.py/main_flet.py, en vez de tener
# que escribirlas a mano en cada terminal nueva. Ver .env.example.
python-dotenv==1.2.3
